# 오픈 전에 확인할 것 리스트

작성일: 2026-07-23 (코드·REPORT.md 기준 실사 후 작성)
기준 코드: backend_9005 (9004 는 미러) + frontend

---

## A. 사업자등록증 → 외부 계약 후 시스템에 추가할 것

> 선행 순서: 사업자등록증 발급 → 각 서비스 신청/심사 → 키 발급 → 아래 코드 작업

### A-1. 휴대폰 본인인증 (인증 중개사 계약: 다날/KG이니시스 등)
- [ ] 중개사 신청·심사 (사업자등록증 필요, 수일~2주, 건당 ~40원)
- [ ] 마이페이지 [본인인증 하기] 버튼에 실제 인증 팝업 연동
  - 현재: `frontend/src/components/Header.jsx:375` — "본인인증 서비스는 준비 중입니다." 안내만 출력
  - 이미 준비됨: is_verified 승격 체계·생년월일/성별 덮어쓰기+잠금 로직 (v121)
- [ ] 인증 결과의 내/외국인 값으로 자기신고 nationality 자동 교정 (v123 에서 설계만 언급)
- [ ] (선택) 14세 미만 자동 판별 강화 — 인증 생년월일 기반

### A-2. 문자(SMS) 발송 계약 (알리고/솔라피 등 + 발신번호 등록)
- [ ] 법정대리인 동의 링크 실발송 연동
  - 교체 지점: `backend_9005/app/services/guardian_notify.py` (현재 mock — 링크 반환만)

### A-3. 보호자 본인인증 (A-1 계약과 동일 건으로 처리 가능)
- [ ] 보호자 동의 페이지의 모의 인증 → 실인증 교체
  - 교체 지점: `backend_9005/app/services/guardian_verify.py` (현재 mock — 즉시 통과)
- [ ] 위 두 어댑터 교체 후 기능 스위치 ON: `.env` 에 `GUARDIAN_CONSENT_ENABLED=true` (config.py:guardian_consent_enabled, 기본 False)
  - ON 전까지 14세 미만 가입은 "준비 중" 안내로 차단됨 (v125 에서 전체 플로우 테스트 완료 상태)

### A-4. OAuth 실키 발급 (구글/네이버/카카오)
- [ ] 각 개발자센터 앱 등록 + 키 발급 → `.env` 주입
  - 현재: `backend_9005/app/config.py:124-129` — google/kakao/naver client_id/secret 전부 빈 값 (라우트·서비스 코드는 3사 완비)
- [ ] **네이버·카카오는 키 발급 시 "이름·생년월일·성별" 동의 항목 검수 신청 필수**
  - 미승인 시: 네이버/카카오 가입자가 "인증 회원"으로만 표시되고 생년월일·성별이 빈 값으로 들어옴 (v121 설계의 전제조건)
- [ ] 실키 적용 후 소셜 가입 실브라우저 E2E 1회 (온보딩 추가정보 화면 포함 — 지금까지 코드 검증만 수행됨)
- [ ] iOS 앱(9004 사용 앱팀)에서 구글 로그인 제공 시 **Sign in with Apple 의무 제공** (애플 정책) — 앱팀 협의

### A-5. Suno API (이미 사용 중 — 결제/한도 점검)
- [ ] sunoapi.org 크레딧 충전 정책·잔액 알림 체계 (베타 트래픽 대비)

### A-5b. Wondera API 접속 차단 (2026-07-23 발견, 2026-07-27 원인 규명)
- [x] 조사 완료: 키 유효(재발급까지 시험)·주소 일치·Bearer 인증 전환·IP 는 KT 한국 일반 회선(Tailscale 무관, 실측) — 그래도 403. **공식 Error Codes 문서상 403 = "unsupported region"(미지원 지역 접근)** → 한국이 API 미지원 지역일 가능성 유력
- [ ] Wondera 지원팀 문의 발송·회신 대기 (문안 전달됨: "Is South Korea a supported region?")
- [ ] "한국 미지원" 확정 시: AWS 해외 리전 이전으로 자연 해결(이전 계획과 연계) 또는 프론트에서 Wondera 숨김
- [x] `POST /v1/song/recognize` 연동 **코드 구현 완료 (v134, 2026-07-27)** — 파이프라인·자막 폴백·라우트(`POST /api/tracks/{id}/recognize-timestamps`, 소유자만) 전부 구축, mock/주입 검증 통과. 실호출만 차단으로 미검증
- [ ] **차단 해제 후 할 일**: ①생성 테스트 ②recognize 실호출 1회 → 자막 영상 확인 → UI 버튼 연결(별도 오더) ③Whisper 자체 파이프라인과 품질 비교 후 채택 결정 (사용자 지시: Wondera 곡 생성 가능해진 뒤 진행)

### A-5c. 얼굴 인증 실연동 (AWS 키 — v139 골격 구축 완료, 2026-07-27)
- [x] ~~AWS IAM 사용자·키 발급·주입~~ → **2026-07-28 완료** — 루트 MFA 설정, 관리자 IAM(jaekyu-admin), 서버용 IAM(aidol-face-verify, RekognitionFullAccess) 생성, 키 .env 주입(9005/9004), 실호출 검증: CompareFaces(서울) 인증 OK + Liveness 세션 생성(도쿄) OK, 서버 기동 mode=aws. AWS 이전 시 키 → EC2 IAM 역할 전환(E 항목 연계)
- [ ] FACE_DATA_KEY(Fernet) 생성·시크릿 보관 정책 확정
- [x] ~~FE Amplify FaceLivenessDetector 통합~~ → **v140(2026-07-28) 완료** (Cognito 풀 생성·liveness-only 권한·웹 통합·tester 5/5). 남은 것: 실기기 E2E 확인 → 정식 ON 은 A-1 후
- [ ] **앱팀 협의(네이티브 앱)**: 앱에도 AWS FaceLivenessDetector **네이티브 SDK**(Android/iOS) 통합 필요 — 웹과 동일한 서버 세션 API(/api/face-verify/session)·동일 Cognito 자격 증명 풀 사용. 웹 통합 완료 후 앱팀에 세션 API 스펙+Cognito 풀 ID 전달
- [ ] 국외이전 동의 문구에 AWS 도쿄(라이브니스) 반영은 face_biometric 동의 전문에 포함됨 — 개인정보처리방침에도 반영 필요(C 항목 연계). 선행: A-1 본인인증 실연동

### A-6. SNS 자동게시 2단계 (플랫폼 API — 각 사 앱 등록·심사 필요)
- [ ] YouTube Data API (앱 심사 + 이용자 채널 OAuth → 비공개 업로드), TikTok Content Posting API (**임시저장 모드** — 앱에서 첨부된 작성화면 대기, 사용자 요구 UX 와 일치), Instagram Graph API (즉시 게시만·비즈니스 계정 한정)
- 1단계(공유영상 생성 + 모바일 공유 시트 + 링크 복사)는 v126 구현 완료 — 2단계는 심사 통과 후 버튼 동작만 교체

### A-7. 앱 추천(리퍼럴) 플레이스토어 링크 교체 (앱팀 앱 출시 후 — v154, 2026-08-03)
- [ ] **`PLAY_STORE_URL` 실제 스토어 URL 로 교체** — 현재 플레이스홀더(`https://play.google.com/store/apps/details?id=com.maidol.app`) 상태
  - 교체 지점: `backend_9005/.env` (+ 9004 미러) 에 `PLAY_STORE_URL=실제URL` 주입 → 서버 재기동 (config.py:play_store_url 이 기본값 오버라이드)
  - 영향 범위: 헤더 📢 공유 모달 공유 링크 + `/invite/{code}` 초대 페이지 [MAIDOL 시작하기] 버튼 — 백엔드 API 응답으로 내려가므로 프론트 수정 불필요
- [ ] (선택) iOS 앱도 출시하면 App Store URL 분기 필요 여부 검토 (현재는 플레이스토어 단일 링크)
- [ ] Kakao SDK 실키 수령 후(A-4 연계) 카톡 공유를 Web Share 폴백 → 카카오링크 리치 카드로 승격 검토

---

## B. 오픈 전 필수 수정 (버그/법적 갭 — 계약 불필요, 바로 가능)

- [x] ~~약관·개인정보 수집 동의 체크박스~~ → **v127 에서 구현 완료** (필수4+선택1 체크, 소셜 온보딩 동의 선행, 기능 시점 동의, 동의 이력 테이블). 아래 후속 2건만 남음:
- [ ] **동의 문구의 `[회사명]`·연락처 플레이스홀더 교체** — 사업자등록(회사명·연락처) 확정 후 `frontend/src/constants/consentTexts.js` 의 플레이스홀더 3곳(이용약관 제1조, 마케팅 전송자)을 실값으로 교체하고 CONSENT_VERSION 을 올려 재배포 (버전이 바뀌면 동의 이력에 새 버전으로 기록됨)
- [ ] **동의 문구 7종 전문 법률 검토** — consentTexts.js 의 이용약관/개인정보 수집·이용/국외이전/만14세/마케팅/사진AI/음성AI 전문을 정식 오픈 전 변호사 검토. 법정 기재요소(개보법 15조 4요소·28조의8 5요소·망법 50조)는 반영·검수 완료 상태이나 표현·범위의 법적 적정성은 전문 검토 필요. 검토 반영 시에도 CONSENT_VERSION 갱신
- [ ] **business.py update_profile 미정의 변수 버그** (v115 tester 발견, 미수정 잔존 — 2026-07-23 재확인)
  - 위치: `backend_9005/app/routes/business.py` update_profile 의 auto-create 분기 (`company_name` 등 미정의 변수 참조 → 그 분기 진입 시 NameError)
  - 프로필 없는 광고주가 PUT /profile 먼저 호출하면 발생 가능
- [ ] **약관 개정 재동의 UX** (v143 등재): 신고·제재 조항(제8조) 추가로 CONSENT_VERSION '2026-07-29.v1' 갱신 — 신규 가입자만 적용됨. 기존 이용자 재동의 팝업(강제 재동의) 정식 오픈 전 결정·구현 필요
- [ ] **신고자 통지 push 승격** (v143 등재): 현재 "내 신고 내역" 조회(pull) 방식 — 알림 인프라 도입 시 처리 결과 개별 통지로 승격
- [ ] **블라인드 곡 직링크 정책** (v141 등재): GET /api/tracks/{id} 는 비공개 곡도 상세 200(기존 설계 — 광고 경로) — 신고 블라인드 곡의 직링크 차단 여부 정책 결정 필요
- [ ] **신고 처리 대상자 통보 승격** (v141 등재): 현재 소유자 화면 라벨 방식 — 알림 인프라 도입 시 개별 통보(임시조치 30일 절차 포함) 구현
- [ ] **포인트 경제 실태 (2026-07-27 운영비 실사 발견)**: ①곡 생성·MV 생성에 포인트 차감 없음(차감은 커버 -2·캐릭터 -2 뿐) — 무료 이용자 생성량 통제 장치 부재(어뷰즈 방어 '레이트리밋' 항목과 통합 해결 필요) ②AdMob 리워드 적립(skip_wait_count)은 SSV 실연동으로 쌓이지만 이를 소비하는 백엔드 코드 미발견 — 소비 로직 구현 또는 적립 의미 재정의 필요
- [ ] **프론트 모델 비용 카드 단가 갱신** (2026-07 웹조사 대비 구버전)
  - 위치: `frontend/src/pages/UploadPage.jsx` 상단 모델 배열들(SCENARIO_MODELS 등)의 perCall 표기 + 영상 모델 카드
  - 예: Seedance 카드 $0.13/초 표기 ↔ 실제 fal.ai standard $0.30/s(720p) (2026-07 조사)
  - 조사 결과는 세션 기록 참조 (Opus 4.7 $5/$25 per M, Veo 3.1 Fast $0.15/s, Kling ~$1.12-1.40/10s, Grok $0.05/s 등)
- [ ] **미해결 버그 (작업리스트2, 이전 세션 이월)**: 가상 캐릭터 탭에서 시도 후 실사화 탭으로 이동해 다시 생성하면 상태가 꼬이는 버그 — 오더 대기 상태로 남아 있음
- [ ] **미사용 외부 연동 3종 비활성/제거** (사용자 확인: 테스트 잔재, 2026-07-23)
  - Sync Labs 립싱크(routes/mv.py + UploadPage), LALAL.AI 보컬복원(routes/vocal_repair.py + api/index.js), Kits AI 음성변환(routes/voice_convert.py + StudioTab2/api)
  - 라우트·UI 가 살아있는 상태로 오픈하면 국외이전 동의 문구(미기재)와 불일치 발생 — 오픈 전 반드시 비활성 또는 문구 재추가 중 택일
- [x] ~~회원탈퇴 익명화에 피드 미포함~~ → **v136(2026-07-27) 해결** — 사용자 확정(인스타 방식): 탈퇴 시 본인 피드·내 댓글·내 좋아요 전부 삭제+카운터 동기, 곡은 "탈퇴한 사용자" 명의 유지

---

## C. 오픈 전 법률/정책 검토 권장

- [ ] 개인정보 처리방침 전면 검토 (변호사/전문 검토 권장) — 특히 신규 수집 항목 반영:
  - 생년월일·성별·지역(선택), 내/외국인, SNS URL, 프로필 사진, CI(본인인증 도입 시), 보호자 이름·연락처(동의 절차), 익명 행적 추적(anon_id)
- [ ] **아동용 쉬운 언어 개인정보 고지** (개보위 아동·청소년 가이드라인) — 보호자 동의 오픈 시 필수
- [ ] 아동(만14세 미만) 대상 맞춤형 광고 제한 정책 문서화
  - 시스템은 이미 대응: insights 집계에서 만14세 미만 행적 자동 제외 (business.py, v125)
- [ ] 위시/클릭 스타 귀속 추적·인구통계 대시보드 제공에 대한 이용자 고지 문구 (개인 식별 불가 집계임을 명시, k-익명성 마스킹 MIN_DEMO_BUCKET=5 적용 중)

---

## D. 비용 실측으로 확정할 것 (추정치 → 실측 전환)

- [ ] gpt-image-2 2048×2048 high 1회 실호출 → usage 토큰으로 실단가 확정 (현재 ~$0.84 는 픽셀 비례 추정)
- [ ] sunoapi.org 대시보드에서 생성 1콜당 크레딧 차감 실측 (현재 V5≈12credits/$0.06-0.12 추정)

---

## E. 운영/인프라 (오픈 준비)

- [ ] AWS 이전 (로드맵 지연 중) — 이전 시 아래 dev 환경 이슈 자동 해소
- [ ] 8/1 베타 오픈 준비 점검
- [ ] (현 dev 환경 한정) /mnt/d(drvfs) 특성: uvicorn --reload·vite 파일워처가 변경 감지 실패 → **코드 반영 시 서버 수동 재시작 필수** (REPORT v118~ 반복 확인). 운영 서버(리눅스 네이티브)에서는 해당 없음
- [ ] 테스트 잔여 데이터 정리 (오픈 전 DB 클린업): wishlist_tester_*/smt_*/fd_*/fdt_*/wfd_*/wft_*/cmt_*/cmti_*/mct_*/tl_*/tli_*/rpt_*/enf_*/fsr_*/tste_*/stv_*/stk_*/tsv_*/fvt_*/lvt_*/mct_*/v117_*/v118_*/v119_*/v120tester/maskchk_*/sns-test-*/gsq_* 계정, 테스트 광고 아이템·이벤트 doc, chk_*/demo_*/mir_* 계정
- [ ] `.env` 실키 주입 후 시크릿 관리 방식 결정 (AWS 이전 시 Secrets Manager 등)
- [ ] **PC 버전 출시 시 SNS 공유 UX 보강** — 데스크톱은 공유 시트(Web Share 파일 첨부) 미지원이라 현재 "다운로드+업로드 페이지 새창" 폴백 동작. PC 버전 낼 때 이 폴백 UX 다듬기(또는 2단계 API 자동게시로 대체)

---

## 완료되어 걱정 안 해도 되는 것 (참고)

- ✅ 광고 이미지 프록시 경로 하드닝 (ads/ 제한, v123)
- ✅ 프로필 이미지 프록시 경로 하드닝 (profiles/ 제한, v122)
- ✅ 보호자 동의 링크 토큰의 접근 로그 마스킹 (v125)
- ✅ 인구통계 소수 버킷 k-익명성 마스킹 (v119)
- ✅ 비로그인 곡 자세히보기 허용 (광고 노출 경로, v120)
- ✅ 개인정보 값(생년월일/성별/보호자 정보/토큰/URL) 로그 미출력 — 각 버전 tester grep 검증 완료

## 신고/제재 후속 (2026-07-30 E2E 중 도출)
- [ ] 허위·악의적 신고자 제재 강화(신고자 스트라이크) — 신고 무기화 실제 방어선. 트래픽/악용 관측 시 도입.
- [ ] get_current_user 밴 재확인(방어 두께, 선택) — 현재는 밴 시 Redis 세션 삭제로 즉시 축출(정석 요건 충족).

## 이용약관·개인정보처리방침 (2026-07-30 v146 작성 완료 → 후속)
- [x] 독립 페이지 /terms·/privacy + 푸터 링크 연결 (전문 legalTexts.js).
- [ ] **정식 시행 전 법률 전문가 검토** (특히 초상권 전용 표준문구, KOMCA 신탁, AI API 위탁vs제3자제공 구분 — 사업자 데이터정책 확인).
- [ ] 유료 결제(PG) 도입 시: 유료서비스(약관 제12조)·청약철회/환불(제13조) 실동작 + 배제조치(미리듣기·체험) UI, 전자상거래법 표시의무.
- [ ] 처리방침 개정 시 시행 7일(중대변경 30일) 전 공지 + 기존 이용자 재동의 UX(CONSENT_VERSION 연계, 미구현).

## 국외이전 AI 사업자 컴플라이언스 조치 (2026-07-30 v146.1 조사 — 위탁 성립 위해 필수)
- [ ] **Google Gemini: 유료(결제 활성화) tier 확인** (무료 tier는 입력 학습+사람검토 → 얼굴/개인정보 부적합).
- [ ] **AWS: AI services opt-out policy 적용 + 도쿄 리전 고정 + 고객 소유 S3** (기본값은 개선 사용+리전외 저장).
- [ ] **Kling: support@klingai.com 학습 옵트아웃 + 위탁계약** (약관상 입력 학습·재라이선싱 → 미조치 시 제3자제공).
- [ ] **fal.ai: 엔터프라이즈/DPA 체결** (표준약관 Usage Data 재사용권), ByteDance 데이터 흐름 서면 확인.
- [ ] ~~SunoAPI.org 교체(공식 Suno로)~~ → **정정: 공식 Suno는 셀프서비스 API 미제공**(2026.7 파트너 API "탐색 중" 관심접수 단계, 보장 아님). 당장 교체 대상 없음.
  - [ ] **당분간 SunoAPI.org(서드파티) 유지** — 단, 실제 넘어가는 데이터 범위(텍스트 프롬프트 vs 업로드 음성) 코드 확인 필요. 텍스트만이면 개인정보 부담 낮음.
  - [ ] **Suno developer API 파트너 신청** (intake form, 승인·시점 불확실 장기옵션). 승인 시 서드파티 대체.
  - [ ] **정식 출시 계획: 음원 생성을 Wondera 위주로 전환 검토**(사용자 방침). ⚠ 단 v146.1 조사에서 **Wondera 데이터 정책 불투명**(공식 개인정보처리방침 인증서 만료·학습/보존 정책 미공개) 확인됨 → **메인 제공자로 쓰려면 데이터 정책·소재·학습사용·DPA를 반드시 서면 확인**(더 많은 데이터가 통과하므로 검증 필수). 확인 안 되면 공식 API 있는 대안(Stable Audio·ElevenLabs Music·Google Lyria 등) 병행 검토.
- [ ] **Wondera 데이터정책·소재 서면 확인** 또는 대체 (공식 처리방침 인증서 만료·정책 불투명).
- [ ] OpenAI/xAI: 학습 옵트인(공유) 설정 켜지 않기 (기본 비학습 유지).
