/**
 * AIDOL(PANN) 디자인 토큰 통합 진입점
 *
 * 사용법:
 *   import { colors, typography, spacing, radius } from '../theme';
 *   // 또는 짧게
 *   import { C, T, S, R } from '../theme';
 */
export { colors, C } from './colors';
export { typography, T, size as fontSize, weight as fontWeight, style as textStyle } from './typography';
export { spacing, S, radius, R, layout } from './spacing';
